/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placehold.co", "www.acouplecooks.com", "images.unsplash.com", "plus.unsplash.com"],
  },
};

module.exports = nextConfig;
